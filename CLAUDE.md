# CLAUDE.md

給接手這個專案的 Claude Code session 看的簡短說明。詳細功能說明給人看的版本在 [README.md](./README.md)，開發進度在 [IMPLEMENTATION_PLAN.md](./IMPLEMENTATION_PLAN.md)。

## 這是什麼

PaperLens：個人用、純前端的學術論文閱讀 + AI 分析工具。核心價值是「AI 給的每個重點都要附原文引用，而且引用是否為真由本機程式碼重新核對，不是相信 AI 自己說的」。

## 技術棧與硬性限制

- React 19 + Vite 5（**不是** TypeScript）+ 純 npm，**不要**引入 CRA / Next.js 之類的框架
- **沒有後端**：不要加 Express、serverless function、Firebase、Supabase 之類的東西。所有資料存 IndexedDB（Dexie），Claude API 直接從瀏覽器呼叫（`dangerouslyAllowBrowser: true`）
- Node 版本相容性：目前釘選 `pdfjs-dist@4.8.69`（4.9+ 需要 Node 20+）、`vite@^5`、`@vitejs/plugin-react@^4`（6.x 需要 vite 8）。如果之後升級這些套件，先確認本機 Node 版本相容，不要假設環境已升級
- 路由用 `HashRouter`（GitHub Pages 沒有 SPA fallback，用 hash route 最簡單可靠）；`vite.config.js` 的 `base: './'` 是刻意的，不要改成寫死的 repo 路徑
- CSS 全部手寫（`src/styles.css`），沒有用 UI 框架（沒有 Tailwind/MUI 等）；圖示用 `lucide-react`
- 絕對不要把 API key 寫進程式碼、console.log、或備份 JSON（`storage.js` 的 `exportBackup()` 特意排除 `settings.apiKey` 和 `papers[].fileBlob`，測試有覆蓋這兩點，改動時務必保留）

## 目錄結構

```
src/
  main.jsx, App.jsx          路由與整體外殼
  styles.css                 全站樣式（手寫 CSS）
  hooks/useSettings.js       讀寫 Dexie settings 表的小 hook
  components/                展示型元件（ui.jsx 放共用 atom：Button/Card/Badge/Modal...）
  pages/                     PapersPage / PaperDetailPage / ComparePage / SettingsPage
  components/ParsedTextPanel.jsx  「解析文字」分頁：debug 用，忠實顯示 IndexedDB 裡的 pages/segments，
                             include/exclude 狀態直接重用 text.js 的 computeExcludedSegmentIds
                             （不要在這裡另外寫一套排除判斷，否則會跟實際送給 Claude 的內容不一致）
  lib/
    storage.js   Dexie schema + CRUD + 備份/還原/清除（唯一碰 IndexedDB 的地方）
    pdf.js       唯一 import pdfjs-dist 的地方：文字擷取、頁面 render 成圖片、開啟 PDF 頁
    text.js      純函式：normalizeText、段落重建、references/appendix 偵測、
                 verifyQuote（本機引用核對的核心）、findQuoteHighlight
    prompts.js   system prompt、五個分析 module 的問題模板、tool schema（結構化輸出用）
    api.js       唯一 import @anthropic-ai/sdk 的地方：呼叫 Claude、model 列表、
                  錯誤訊息翻譯成中文、usage 欄位擷取
    ingest.js    上傳 PDF 之後的整合流程（extractPdfText → detectScannedPdf →
                  detectReferences → 存進 Dexie）
    export.js    Markdown 匯出、下載/剪貼簿小工具
```

新增邏輯時盡量放進對應職責的 lib 檔案，不要全部塞進 page component。

## 「原文核對」的資料流（最重要的部分，改動前一定要先懂這段）

1. `pdf.js: extractPdfText()` 用 pdfjs 逐頁抓文字，依幾何位置（行距/縮排）判斷段落邊界
2. `text.js: buildParagraphsFromLines()` 把行組成段落字串（處理斷字、多餘空白）
3. `text.js: buildSegments()` 給每個段落一個穩定 ID：`p{page}-s{index}`（例如 `p3-s12`）
4. 分析時，`prompts.js: buildDocumentXml()` 把 segments 包成 `<document><segment id="..." page="...">...</segment>...</document>` 送給 Claude；Claude 被要求引用時必須附 `segmentId` + `page` + 盡量逐字的 `quote`
5. **拿到 Claude 回應後，不能直接相信它**：`text.js: verifyQuote(quote, segments, {hintSegmentId, hintPage})` 會重新在 `paper.segments` 裡搜尋這段 quote（normalize 後做 exact substring 比對；找不到再用詞序 LCS 做 fuzzy 比對；順序是「先看 Claude 指定的 segment → 同頁 → 全文」），回傳 `exact` / `partial` / `not_found`，並且**以本機找到的頁碼為準**，不是 Claude 說的頁碼
6. UI（`AnalysisModule.jsx`、`QuestionPanel.jsx`、`EvidenceModal.jsx`）永遠是即時呼叫 `verifyQuote` 來畫核對狀態，**不會**把 verification 結果存進 Dexie —— 這樣以後改進 `verifyQuote` 演算法時，舊資料的核對結果會自動用新邏輯重新算，不需要跑 migration

如果要改進比對演算法，改 `text.js` 就好，UI 端不需要動。`src/test/text.test.js` 有覆蓋 exact / partial / not_found / page mismatch / references+appendix 偵測，改動後務必跑 `npm test`。

## 結構化輸出策略

`api.js: callStructured()`：優先用 tool-forced call（`tool_choice: {type:'tool', name:...}`），SDK 已經把 `tool_use.input` 解析成物件，不用自己 parse JSON。如果模型沒回 tool_use block（例如某些 custom model），才 fallback 成「純文字裡要求輸出 JSON」+ `parseJsonLoose()`（會處理 ```json fence）。改動這段時要保留這個 fallback，因為使用者可以填任意 custom model ID。

## Prompt caching

`api.js` 裡 `system` 和 `<document>` 這個 content block 都掛了 `cache_control: {type:'ephemeral'}`；每個 module 的問題文字（含研究背景）故意放在 document **之後**、不掛 cache_control，這樣連續分析五個面向時，document 這段可以被快取重複利用。這是成本優化，不是必要功能——如果之後 Anthropic API 變了，寧可讓 caching 失效也不要讓分析功能壞掉。改動前先看 `node_modules/@anthropic-ai/sdk` 目前實際的型別定義，不要憑記憶寫舊版 beta header（目前這版 SDK 的 prompt caching 已經是穩定 API，不需要 beta header）。

## 測試

`npm test`（= `vitest run`，非 watch mode）。除了 `src/test/text.test.js`、`src/test/storage.test.js`（用 `fake-indexeddb` 跑真的 Dexie）之外，`src/test/pdf.integration.test.js` 會用 `src/test/fixtures/sample.pdf`（手刻的最小合法 PDF）真的跑一次 pdfjs 抽取流程，不是 mock。這個測試需要 `src/test/setup.js` 裡的兩個 polyfill（`Promise.withResolvers`、worker 路徑指到 node_modules 的實體檔案）——這些只影響 Node 測試環境，不影響瀏覽器裡的實際行為，不要因為「看起來多餘」就刪掉。

## 已知會讓人驚訝的設計決定

- 段落 **不跨頁**：每個 segment 只屬於一頁，簡化了「頁碼永遠明確」這件事，代價是硬跨頁的段落會被拆成兩段
- `extractionMode` 初始一律是 `'text'`，只有使用者手動跑完 OCR 之後才會變成 `'ocr'`——即使偵測到疑似掃描 PDF 也一樣，因為換成 OCR 模式代表「引用核對已經改成比對 OCR 文字」，這件事必須是使用者主動觸發的，不能自動發生
- `computeExcludedSegmentIds()` 每次都是即時算的（不存 Dexie），使用者在「解析資訊」分頁勾選「取消排除參考文獻」只是切換 `referenceInfo.userDisabled` 這個 flag
- 「數據細節」模組的 analysis item 多一個獨立欄位 `sourceScope`（`paper` / `reviewed_studies` / `synthesis` / `unclear`，定義在 `prompts.js`），跟 `kind`（fact/inference/not_mentioned）是兩個不同維度，不要混在一起判斷。目的是避免 systematic review 裡「被回顧研究的數據」被誤讀成「這篇論文自己做出來的數據」。只有 `runModuleAnalysis()` 在 `moduleId === 'data'` 時才會用 `normalizeSourceScope()` 把它 normalize 成合法值（missing／非法值一律變成 `'unclear'`，不會 throw）；其他模組、自訂提問、跨篇比較都不處理這個欄位。UI（`AnalysisModule.jsx`）也是用同一個 `normalizeSourceScope()` 來決定 badge 文字，所以舊資料（沒有 `sourceScope` 欄位）會自動顯示「來源層級不明」而不是 crash——沒有為這個欄位做 IndexedDB migration，也不需要，因為它本來就是 analysis item JSON 裡的 optional field。
- **資料安全稽核（2026-09-11）**：使用者回報整個 IndexedDB（papers、analyses、甚至 API key）在隔天消失。全專案稽核後，**沒有找到**任何 app 程式碼會清空、刪除或重建整個 database——`clearAllPaperData()` 是唯一的批次清除函式，只從 `SettingsPage.jsx` 一個按鈕（背後有 `confirm()`）呼叫，且刻意不碰 `settings` table，所以它本身無法解釋 API key 也消失。最可能的真正原因是瀏覽器層級的事情，不是 app bug：(a) `http://127.0.0.1:5173` 與 `http://localhost:5173` 是不同 origin，IndexedDB 完全分開儲存；(b) 這個 app 從未呼叫過 `navigator.storage.persist()`，所以瀏覽器可能在儲存空間不足、或使用者開啟「關閉瀏覽器時清除網站資料」這類隱私設定時，把整個 origin 的資料當作「best-effort」直接清掉，且不會有任何 app 端的紀錄可查。稽核中確實抓到一個真實的資料遺失 bug（已修正）：`storage.js: saveAnalysis()` 原本在收到 `{status:'error', items:[]}`（重新分析失敗時 `PaperDetailPage.jsx` 的 catch block 會這樣呼叫）時，會用空陣列覆蓋掉該 module 原本已存在、成功的分析結果——因為 `data.items || []` 對空陣列 `[]` 不會 fallback（`[]` 在 JS 是 truthy）。現在改成：只有在「這次是錯誤、而且呼叫者沒有帶新的 items」時才保留舊有的 `items`，真正成功的儲存（即使結果剛好是空陣列）仍然會正常覆蓋。詳見 `src/test/dataSafety.test.js`。另外也在 `main.jsx` 加了 `requestPersistentStorage()`（fire-and-forget，最佳努力，不保證生效），並在「設定」頁加了本機資料儲存的提醒與持久性儲存狀態顯示。
- 「可復現性」模組**沒有**固定 checklist，也**沒有**新增 schema 欄位（item 形狀仍是 `{id, title, claim, kind, evidence}`）。改動完全在 `prompts.js` 的 prompt 文字：要求 Claude 先判斷 paper type（一般 ML/experimental、system/IoT/hardware、systematic review/literature review/survey、meta-analysis），再依 paper type 動態決定要檢查哪些項目，並嚴格區分「不適用」（該項目對這種 paper type 根本不相關，直接不產生卡片）跟「原文未提及」（`kind='not_mentioned'`，這項資訊本來就該有復現價值、缺了才會影響重現）。所以同一批 checklist 名詞（hardware、random seed、topology…）不會每篇論文都被套用——這是 prompt 指示 Claude 的行為，不是程式碼在事後過濾卡片，因此 `src/test/reproducibility.test.js` 只能做結構性的字串斷言（例如 prompt 是否真的提到「先判斷這篇論文的類型」「不適用」等關鍵規則），無法真正驗證 Claude 實際輸出的動態行為。
- 「弱點與延伸」模組的 analysis item 多一個獨立欄位 `weaknessType`（`author_limitation` / `scope_choice` / `inferred_limitation` / `field_challenge` / `future_direction`，定義在 `prompts.js`），跟 `sourceScope` 是同一種設計模式，但**沒有** `unclear` 這種 catch-all 值——`normalizeWeaknessType()` 對缺失/非法值一律回傳 `undefined`，`AnalysisModule.jsx` 只在拿到合法值時才畫第二個 badge，否則就不畫（不是畫一個「不明」的 badge）。這個欄位存在的目的是把「作者自己承認的弱點」（`author_limitation`）和「作者設定的研究範圍／納入排除條件」（`scope_choice`）分開——後者不是弱點，只是研究設計選擇，prompt 明確禁止把 scope_choice 自動升級成 author_limitation；同樣也把「這篇 review 自己的限制」和「作者在 Discussion 裡討論的領域挑戰」（`field_challenge`）分開。`runModuleAnalysis()` 只在 `moduleId === 'limitations'` 時才會 normalize 這個欄位。
- **全站功能 + 資料安全稽核（2026-09-11 第二次）**：這次稽核在 `OcrPanel.jsx: persistResults()` 抓到一個跟 `saveAnalysis()` 同類型的資料遺失 bug——對已經 OCR 成功的論文按「重新辨識全部頁面」，如果這次重跑對某些頁（或全部頁）失敗，舊版程式碼會直接用這次的（不完整或空的）結果覆蓋 `paper.pages`/`paper.segments`，等於用一次失敗的重試銷毀先前成功的 OCR 文字。現在 `persistResults()` 會先把這次「真的成功」的頁面結果和 `paper.pages` 裡「原本就有」的頁面文字合併（新結果優先），任何一頁只要這次沒成功但之前有文字，就保留舊文字；如果合併後完全沒有可用文字才會整個不寫入。也修了 `ComparePage.jsx` 裡一個更隱蔽的正確性問題：送進比較 prompt 的 `verified` 屬性原本是 `item.kind === 'fact'`（只看 kind，沒有真的核對過 quote），第一次修正把它改成 `kind === 'fact'` 且至少一筆 evidence 經 `verifyQuote()` 核對為 `exact` **或 `partial`**——但 partial 代表「部分符合，仍需人工確認」，語意上不該算「已驗證」。目前最終版本：`text.js` 新增 `bestVerificationStatus(evidence, segments)`，回傳 `exact` / `partial` / `not_found` / `unavailable`（沒有 evidence 可核對時）四選一（取多筆 evidence 中最好的結果，優先序 exact > partial > not_found），`verified` 現在是嚴格衍生值：只有 `kind === 'fact' && verificationStatus === 'exact'` 才是 `true`。`buildComparisonUserContent()` 兩個屬性都會送給 Claude（`verificationStatus` 給細緻語意，`verified` 給簡單布林判斷），`COMPARISON_SYSTEM_PROMPT` 也明確寫了「partial 不算 verified」。`verifyQuote()` 本身完全沒有被動過，`bestVerificationStatus` 只是在它之上做「多筆 evidence 取最佳結果」的聚合。另外把「選 2-10 篇論文」的邊界檢查抽成 `text.js: validateComparisonSelection()`（純函式，`ComparePage.jsx` 的勾選上限與送出比較共用同一份訊息），並補上「已刪除論文不會讓比較流程 crash」的防呆（`papers.find(...)` 找不到就跳過，不會對 `undefined` 存取 `.title`）。
- 錯誤／成功訊息 UI：`ui.jsx` 的 `ErrorAlert` 已經改成透過新元件 `InlineNotice`（無邊框、無底色的小行內提示，只有 icon + 有色文字）實作，取代原本一大片 `.alert.alert-danger` 色塊。**持久性的重要提醒**（API key 安全提醒、本機儲存提醒、OCR 會消耗 API 額度的提醒）刻意保留原本的 `.alert` boxed 樣式，這是有意的例外，不是漏改——這幾個是使用者在採取行動「之前」就該注意的事，跟分析/上傳/比較「之後」才出現的一次性回饋訊息，性質不一樣。
- `ComparePage.jsx` 的論文清單會顯示**全部**已上傳的論文，不會因為某篇還沒分析就整篇從畫面消失（舊版這樣做，會讓使用者以為上傳壞掉）。`analysisCount === 0` 的論文仍然列在清單裡，只是 checkbox 是 `disabled`、文字淡化並顯示「尚未分析」；`toggleSelect()` 對這類論文的「加入選取」也有一層防呆（即使 checkbox 沒被 disabled 擋住，也不會真的被加進 `selected`）。這確保了 `selected` 永遠不會含有 0 分析的論文，`handleCompare()` 的核心邏輯（只送已保存 analysis items、`usablePaperCount` 檢查）完全沒有變。
- **關鍵 bug（2026-09-11 第三次）：長論文分析「無聲失敗」**。根本原因在 `api.js: callStructured()`——當 Claude 的 tool-use 回應在生成到一半時被 `max_tokens` 截斷（`response.stop_reason === 'max_tokens'`），API 仍然會回傳一個「結構上合法但內容不完整」的 `input`（可能整個是 `{}`，完全沒有 `items` 欄位）。舊程式碼只檢查 `typeof toolUse.input === 'object'`，空物件也符合這個條件，所以被當成「合法但剛好是空的」分析結果直接回傳成功，`analyzeOne()` 從沒進入過 catch block，`saveAnalysis()` 存的是 `{status:'done', items:[]}`——不是 error，UI 因此完全不會顯示任何錯誤，只是因為 `items.length === 0` 讓 `AnalysisModule.jsx` 判斷成「尚未分析」，看起來像是「跑了 40 秒後無聲重置」。修法：`callStructured()` 新增 `assertNotTruncated(response)`，只要 `stop_reason === 'max_tokens'` 就直接 throw 清楚的錯誤（不管 tool-use 還是 fallback JSON 路徑都會檢查），把「回應被截斷」從一個看似成功的空結果，變成一個真的會被 `analyzeOne()` 的 catch block 抓到、存成 `status:'error'`、顯示給使用者的錯誤。同時把 `runModuleAnalysis` 用的 `max_tokens` 從 4096 提高到 8192（降低長論文因為重點多而被截斷的機率）、`createClient()` 加上 `maxRetries: 0`（SDK 預設會靜默重試 2 次，改成失敗立刻讓使用者自己按「重新分析」）、所有 `.create()` 呼叫加上 `timeout`（`REQUEST_TIMEOUT_MS = 120000`，避免無限期卡住又不會設太短）。另外新增 `prompts.js: normalizeAnalysisItems()`，讓單一格式錯誤的 item（例如 `kind` 不在合法列表）被丟棄而不是讓整批分析失敗，但**不會**幫忙捏造或修補缺漏的 quote。`PaperDetailPage.jsx: analyzeOne()` 的 catch block 也包了一層內層 try/catch，就算連「儲存錯誤狀態」這個動作本身都失敗，也不會變成一個 unhandled rejection 讓 UI 又一次無聲重置。debug 用的輸入大小（segments/characters/estimatedTokens）只在 `import.meta.env.DEV` 下用 `console.debug` 印出，不含 API key 或論文全文。詳見 `src/test/apiFailureModes.test.js`（mock SDK，不呼叫真實 API）。
- **同一個 bug 的第二層（2026-09-11 第四次）**：使用者直接在 IndexedDB 裡看到 `status:"done", items:[], error:null` 且 `usage` 有真實 token 數字——代表 API request **真的完成**了（不是上一輪修的 `stop_reason==='max_tokens'` 截斷），但最終還是存成 0 個 items 的「成功」。這證明只擋截斷是不夠的：Claude 也可能在一個完整、沒有被截斷的回應裡，就是回了 `items: []`，或是 `normalizeAnalysisItems()` 把每一筆都判定成不合法（例如全部 `kind` 都不在合法列表）而清空。這兩種情況 `stop_reason` 都不是 `max_tokens`，舊的 `assertNotTruncated()` 完全抓不到。修法是在 `runModuleAnalysis()` 最後（所有 sourceScope/weaknessType 處理都做完之後）加一個**跟截斷偵測完全獨立**的硬性檢查：只要 `finalItemCount === 0` 就直接 throw「Claude 已回應，但沒有產生可用的分析結果。請重試。」——不管 items 是空是因為截斷、Claude 自己回空、還是 normalize 把全部 item 都判掉，都一律視為失敗，五個分析面向都不允許 0 items 算成功。同時把 `prompts.js: normalizeAnalysisItems()` 的回傳值從單純陣列改成 `{ items, rejections }`（`rejections` 記錄每個被丟掉的 item 的 index/reason，目前只有兩種真的會讓整個 item 被丟掉：不是物件、`kind` 不合法），讓 `api.js` 能在 `import.meta.env.DEV` 下印出 `[module analysis debug]`（module / raw response type / tool_use blocks / raw item count / parsed item count / normalized item count / rejected item count / final item count / rejectReasons），方便下次同類問題直接看 console 就知道 items 在哪一步消失，不必再靠猜的。另外 `AnalysisModule.jsx` 補了一個小提示：`kind='fact'` 但沒有任何 evidence 的 item，現在會顯示「缺少可核對原文」（小行內警示，不是大色塊），不再讓這種 item 看起來跟有證據的 fact 一樣。
- **同一個 bug 的第三層（2026-09-11 第五次）：「分析 X/5」計數沒有排除舊的 done+[] 假成功紀錄**。`storage.js` 新增 `isAnalysisCompleted(analysis)`：`status === 'done' && Array.isArray(items) && items.length > 0` 才算完成——這是**唯一**判斷「這個 module 算不算完成」的地方，`PapersPage.jsx`（首頁卡片計數）、`PaperDetailPage.jsx`（頁首計數、「分析全部未完成面向」的 pending 清單）、`ComparePage.jsx`（論文是否可選、以及送進比較 prompt 的 items 來源）全部改用這個函式，不能各自維護一套判斷邏輯。`AnalysisModule.jsx` 對 `status==='done' but items:[]` 的舊紀錄，改顯示「先前分析沒有取得完整結果，請重新分析。」（`InlineNotice`），不會顯示成「完成」。這只是相容既有壞資料的顯示邏輯，**不做任何 destructive migration**——舊紀錄還在 Dexie 裡，只是不再被誤判為完成；使用者重新分析成功後，新結果會正常覆蓋它。（下一條記錄取代了這裡原本「status==='error' 一律不算完成」的說法——見下。）
- **重新分析失敗的語意再修正（2026-09-11 第六次）**：上一條記錄裡「失敗重試會保留 items 但 status 變成 error」的設計，其實跟「保留舊結果」的初衷互相矛盾——一個已經成功、使用者也還看得到完整內容的 module，只因為使用者手癢按了一次重新分析又剛好失敗，就被 `isAnalysisCompleted()` 判定為未完成、從 X/5 扣掉、也不能再送進跨篇比較，這不合理。`storage.js: saveAnalysis()` 重新設計：**「目前保存的成功結果狀態」跟「最近一次分析嘗試是否失敗」是兩個獨立欄位**。如果這次儲存是失敗（`status:'error'` 且沒有新 items）、而且原本已經有 `isAnalysisCompleted()` 為真的成功結果，就維持 `status:'done'`、`items` 完全不動，失敗訊息改記錄在新欄位 `lastAttemptError`／`lastAttemptAt`（`error` 欄位設回 `null`）；只有「根本沒有任何成功結果可以保留」時（第一次分析就失敗，或先前也從未成功過）才會真的存成 `status:'error'`。下一次重新分析成功時，`items`／`status` 正常更新，`lastAttemptError`／`lastAttemptAt` 一併清空。因為 `isAnalysisCompleted()` 本身完全沒有改（還是只看 `status==='done' && items.length>0`），這個修正不需要碰 `ComparePage.jsx`／`PapersPage.jsx`／`PaperDetailPage.jsx` 任何一處計數邏輯，它們會自動正確運作。`AnalysisModule.jsx` 新增一個獨立的 `InlineNotice`：`isAnalysisCompleted(analysis) && analysis.lastAttemptError` 時顯示「最近一次重新分析失敗，目前顯示上一次成功結果。」（跟成功內容一起顯示，不是取代它）。舊資料沒有 `lastAttemptError`/`lastAttemptAt` 欄位完全沒問題（`undefined` 讀取正常，条件判斷自然為 false），不需要 migration。
- 同一輪也修正 `prompts.js: normalizeAnalysisItems()`：`claim` 缺失、非字串、或 trim 後為空——**整個 item 必須被 reject**（原本只是給空字串預設值），因為 claim 是分析的實際內容，PaperLens 不該幫 Claude 沒寫的分析敘述捏造內容。`title` 缺失則維持安全 fallback（現在是「分析項目」），因為 title 只是標籤不是分析內容，這個區分是刻意的。`evidence` 格式錯誤的處理沒變：只丟掉那一筆 evidence，不會連累整個 item。
- **`analysis.items.map is not a function` crash（2026-09-11 第七次）**：「弱點與延伸」點進去整頁 crash。根本原因是 `analysis.items` 存進 IndexedDB 時不是陣列（很可能是 Claude 把 5 種 weaknessType 當成分組 key，回傳類似 `{ author_limitation: [...], scope_choice: [...] }` 的物件，而不是我們 schema 要求的扁平 `items` 陣列——limitations 的 prompt 詳細列了 5 個分類，容易把模型帶偏成用分類分組輸出）。舊碼在這條路徑上有漏洞：`api.js: normalizeStructuredData()` 原本遇到 `!Array.isArray(data.items)` 時會直接 `return { data, diagnostics: null }`——**把整個 `data` 原封不動傳回去**，`data.items` 依然是那個物件；雖然下游 `runModuleAnalysis()` 的 `finalItemCount===0` 檢查理論上會抓到並 throw，但這個函式本身沒有守住「回傳的 items 永遠是陣列」這個 invariant，任何漏接或未來新增的呼叫路徑都可能讓非陣列值溜過去。現在 `normalizeStructuredData()` 只要 `data` 有 `items` 這個 key，回傳的 `data.items` 保證永遠是陣列（非陣列一律變 `[]`，並記一筆 `{reason: 'items_not_an_array'}` 到 diagnostics）。`storage.js: saveAnalysis()` 也加了同等級的 invariant，**完全不信任呼叫者**：只有 `Array.isArray(items) && items.length>0` 才算真的成功，就算呼叫者傳 `status:'done'` 也一樣會被導去「失敗」分支（沿用「保留舊成功結果」或「存成 error」的既有邏輯，訊息預設「Claude 回傳的分析格式不完整，請重新分析。」）。`AnalysisModule.jsx`／`QuestionPanel.jsx`／`ComparePage.jsx`／`export.js`／`text.js: verifyComparisonSources()` 全部補上 `Array.isArray()` defensive rendering（不是簡單 `|| []`，因為非空字串等 truthy 非陣列值會繞過 `|| []`），舊資料如果 `items` 不是陣列，`AnalysisModule.jsx` 會顯示「先前分析資料格式異常，請重新分析。」而不是 crash 或誤判成完成。`prompts.js` 的 limitations prompt 也加了一句明確提醒：輸出必須是扁平 `items` 陣列，不要用 weaknessType 分類名稱當 key 分組。詳見 `src/test/malformedItemsShape.test.js`。
- **跨篇比較的 max_tokens 截斷（2026-09-11 第八次）**：使用者選 2 篇都已完成 5/5 面向的論文、沒填聚焦問題，比較就撞到 `stop_reason==='max_tokens'`。根本原因不只一個：(1) `api.js: runComparison()` 原本明確傳 `maxTokens: 4096`——比 `callStructured()` 給單一分析模組用的預設值 8192 還低，明顯反了，因為比較要消化的 item 數量遠多於單一模組分析；(2) 更根本的是完全沒有任何機制限制**輸出的形狀**——prompt 沒有要求數量上限，導致 Claude 可能為每個 analysis item 都各自產生一張比較卡片，論文數一多輸出就線性膨脹；(3) 方法／範圍不同的東西（例如「A 用方法 X，B 用方法 Y」）被硬塞進 `contradiction`，讓分類本身也不夠精準。修法刻意不是「單純調高 max_tokens」（那治標不治本，10 篇論文一樣會炸），而是三層一起改：
  - **輸入瘦身**：`text.js` 新增 `pruneComparisonItems(items, {maxNotMentionedPerGroup=3})`——同一篇論文同一個 module 底下，完全重複的 claim 只留一筆，`kind==='not_mentioned'` 的項目每組最多保留 3 筆（不是整批刪除，只是不讓大量「原文未提及」的 gap 無限堆疊吃掉 token）。`ComparePage.jsx: handleCompare()` 在建立 `validKeys`/`moduleByKey`（本機來源驗證用的合法 ID 集合）**之前**先做這次 prune，確保「Claude 實際被允許引用的 ID」跟「本機驗證接受的 ID」完全一致；`api.js: runComparison()` 內部對傳入的 `items` 再 prune 一次（冪等，不會重複刪除），這樣即使未來新增別的呼叫路徑忘記在呼叫端 prune，也有一層防護。順帶確認：送進比較 prompt 的 item 本來就**不含**任何原文引用（`evidence`），只有 `title`／`claim`／`kind`／`verificationStatus`／`verified` 等 metadata，這部分完全沒變。
  - **輸出瘦身（prompt-level，不是程式碼硬截斷）**：`prompts.js: COMPARISON_SYSTEM_PROMPT` 全面重寫，新增規則 7：沒有聚焦問題時每種類型（consensus／difference／contradiction／research_gap）預設最多各挑 4-5 個發現，不需要每個 item 都對應一張卡片；每個 finding 的 `summary` 限制 1-3 句話，不重複整段原始引用或整篇單篇摘要；規則 2 同時把 `sources` 限制在「每篇論文 1-2 個最直接來源」，不把同主題所有相關 item 都塞進來。`COMPARISON_TOOL` 的 `description` 與各欄位 `description` 也同步加上這些限制的文字說明，讓 tool schema 本身也在提醒模型。有聚焦問題時，規則 6 明確要求「只回答與聚焦問題直接相關的比較，不要延伸到其他主題」。
  - **新增 `difference` 分類**：`prompts.js` 新增 `COMPARISON_TYPES = ['consensus','difference','contradiction','research_gap']` 與 `COMPARISON_TYPE_LABELS`（`difference` → 「差異」），`COMPARISON_TOOL` 的 `type` enum 同步更新，`COMPARISON_SYSTEM_PROMPT` 規則 4 詳細寫清楚三者界線：方法／範圍／資料等選擇不同只能標 `difference`，`contradiction` 只保留給「同一個問題、同一個現象、同類條件」下實質不相容的結果。`ComparePage.jsx` 的 `TYPE_LABEL` 直接重用 `COMPARISON_TYPE_LABELS`（不再自己維護一份，避免以後兩邊漂移），`TYPE_BADGE` 新增 `difference: 'partial'`（沿用既有 `Badge` 的 warning 配色，跟 consensus=綠、contradiction=紅、research_gap=藍區分開）。**舊資料完全相容**：舊的比較紀錄裡的 `type` 只會是 `consensus`／`contradiction`／`research_gap`，這三個值本來就還在 `COMPARISON_TYPE_LABELS`／`TYPE_BADGE` 裡，沒有任何 migration，舊卡片會照常正確顯示。
  - **budget 與獨立錯誤訊息**：`api.js` 新增常數 `COMPARISON_MAX_TOKENS = 8192`（取代原本的 4096，理由是「有了輸入/輸出瘦身之後，這個 budget 才是有意義的餘裕，不是唯一防線」）。`assertNotTruncated(response, message)` 改成可以接受呼叫端自訂訊息（不再是寫死字串），`runComparison()` 傳入專屬的截斷訊息：「比較結果太長，AI 回應在完成前被截斷。可以減少比較論文數量，或填寫『聚焦問題』縮小比較範圍後再試一次。」——不再沿用單一論文分析那句「縮小分析範圍／排除內容」，因為比較本來就不會重新讀 PDF，那句話對使用者沒有意義。`stop_reason==='max_tokens'` 的安全網完全沒動：截斷一律 throw，不會被當成部分成功存起來；也沒有任何自動重試；失敗的比較嘗試完全不會影響、也不會刪除已經存在的舊比較紀錄（`saveComparison()` 每次都是獨立新建一筆，從來不會 upsert 或牽動既有紀錄）。`callStructured()` 也同步補了 `findings` 欄位的陣列 invariant（跟 `items` 同一套防護邏輯）與回傳值裡的 `stopReason`，讓 dev-only 的 `console.debug('[PaperLens] Comparison input:', {...})`（`papers`／`analysisItems`／`characters`／`estimatedInputTokens`／`maxOutputTokens`／`stopReason`，不含 API key、論文全文或組好的 prompt）能在一次呼叫後就印出完整結果，方便下次直接從 console 判斷是輸入太大還是別的問題。詳見 `src/test/comparisonTokenBudget.test.js`（mock SDK，涵蓋截斷不算成功、2/10 篇論文 prompt builder 不 crash、`difference` 端到端正確、來源驗證與 prune 的交互、API 失敗不刪除舊紀錄等情境）。
- **跨篇比較產生空結果（2026-09-11 第九次）**：上一輪修完 max_tokens 之後，使用者拿兩篇真的完成 5/5 分析、主題有實質重疊（都在講 wearable physiological sensing 用於壓力／健康監測，一篇偏 systematic review、一篇偏 PPG 深度學習）的論文去比較，API 沒有錯誤也沒有截斷，比較「成功保存」，但畫面完全沒有卡片，只顯示「沒有發現任何共識、矛盾或研究缺口。」——這句文案本身也漏了新增的「差異」類型。完整追過一次 pipeline（selected papers → getAnalyses → isAnalysisCompleted → pruneComparisonItems → comparison prompt → Claude tool-use output → parse → normalize → type 驗證 → sourceId 驗證 → verifyComparisonSources → final items → saveComparison → UI render）之後，**確認當時的程式碼裡沒有任何一步會把一個非空的 `findings` 陣列整批清空**：`verifyComparisonSources()` 只會拿掉單一 finding 裡無效的 source、絕不會因為部分 source 無效就砍掉整張卡片；而且當時完全沒有任何程式碼驗證 `finding.type` 是否落在合法列表裡（所以「difference 被舊的三分類 filter 誤刪」這個猜測本身並不成立，因為根本沒有這一層 filter 存在）。這代表最可能的解釋是 **Claude 自己的原始輸出就是空的（或幾乎是空的）**——上一輪重寫的 `COMPARISON_SYSTEM_PROMPT` 為了避免幻覺／灌水，疊了大量「寧可少而準」「不能假裝知道」「不算 contradiction」之類的保守用語，卻從來沒有明確告訴 Claude「找到一些不完美但誠實的發現，遠比完全不輸出更有價值」，讓模型在真實世界、沒有完美契合的資料下，傾向直接交出空清單。這一輪的修法涵蓋三個獨立問題：
  1. **補上一層真正的 per-finding 驗證，讓「difference 被誤刪」這種情境未來不可能無聲發生**：`prompts.js` 新增 `normalizeComparisonFindings(rawFindings)`，跟 `normalizeAnalysisItems()` 同一套設計——只有結構性問題（不是物件、`type` 不在 `COMPARISON_TYPES` 裡、缺 title／summary）才整筆 reject，個別 source 格式錯誤只丟該筆 source，不連累整個 finding；並回傳 `{findings, rejections}` 讓 debug log 看得到每筆被丟掉的理由。`api.js: normalizeStructuredData()` 對 `findings` 欄位也比照 `items` 欄位接上這層驗證（之前只保證陣列形狀，完全沒做逐筆驗證），這樣 `COMPARISON_TYPES` 變成唯一定義、也是唯一驗證 enum 的地方，不會再有「某處還停留在舊三分類」這種分裂風險。
  2. **重新定義「完全沒有有效來源」的 finding 要整張捨棄，但只捨棄這種情況**：`text.js` 新增 `dropUnsourcedComparisonFindings(findings)`——一張卡片經過 `verifyComparisonSources()` 驗證後，如果 `sources` 變成空陣列（不論是原本就沒附來源，還是附的來源全部造假／對不上），代表這張卡片沒有任何可回溯的依據，予以整張捨棄；但只要還剩至少 1 個真實來源就完整保留，不會因為「部分來源無效」就整張砍掉（這是既有行為，這次只是把它抽成獨立、可單獨測試的函式，並補上「全部無效才砍」的新規則）。四種 type（consensus／difference／contradiction／research_gap）套用同一套規則，沒有例外；`COMPARISON_TOOL` 的 `sources` 欄位描述也加了一句提醒：即使是 research_gap 這種「你自己整理出來的觀察」，也至少要附 1 個來源，不能完全不附。
  3. **不能再把「Claude 真的產生 0 個發現」跟「Claude有產生、但全部被本機處理過程清掉」混為一談**：這是跟 `runModuleAnalysis()` 的 `finalItemCount===0` guard 同一種「假成功」防護，但比較這邊一直缺這一層。`ComparePage.jsx: handleCompare()` 現在會保留 `diagnostics.rawItemCount`（Claude 原始回應、經過 type/shape 驗證前的 finding 數），跟本機處理完（source 驗證 + 去空來源）之後的最終數量分開追蹤：**`rawItemCount === 0` 是合法、可以存的空結果**（Claude 真的判斷沒什麼好比的）；但 **`rawItemCount > 0` 而最終結果變成 0**，代表資料在本機驗證階段整批陣亡（例如整批都引用了不存在的 source ID，或整批 `type` 不合法），這種情況現在會直接 `throw`，顯示錯誤訊息「比較處理過程發生異常：AI 產生的比較結果在驗證後全部無法使用……」，**不會**被當成正常的空結果存起來。同時新增 dev-only 的 `console.debug('[PaperLens] Comparison debug', {...})`（涵蓋 selected papers／input analysis items／items after pruning／raw comparison item count／parsed item count／normalized item count／invalid-type count／invalid-source count／items after source validation／final item count／依 type 分類的數量／完整的 rejection 清單，不含 API key、論文全文或組好的 prompt），跟既有的 `[PaperLens] Comparison input:`（輸入大小／`stopReason`）互補，之後同類問題可以直接從 console 一次看完整條 pipeline 在哪一步變成 0。
  4. **針對過度保守的 prompt 本身動刀**：`COMPARISON_SYSTEM_PROMPT` 新增規則 8，明講「少而準」不等於「應該回傳空結果」——只要論文之間在應用領域、方法、資料、裝置、模型或結論上有實質重疊或有意義的不同，就足以整理成至少一則 consensus 或 difference，並舉了跟使用者實測案例同類型的具體例子（兩篇論文只要同樣在討論穿戴式裝置生理訊號偵測壓力／健康狀態，這件事本身就足以構成一則 consensus）；規則 9（原規則 8）加了一句「即使結論是『只有少數發現』，也要如實列出，不要因為數量少就乾脆不輸出」。`buildComparisonUserContent()` 沒有聚焦問題時的預設文字也補了一句同樣方向的提醒。這一層修正沒有辦法用 mock SDK 100%證明「Claude 之後就不會再回空」（那需要真的呼叫 API），所以這次同時做了 (1)-(3) 三層防護：即使 prompt 調整後 Claude 偶爾還是回得很保守，至少「真的 0 個」跟「處理過程中被清空」這兩種情況不會再被混在一起、也不會再無聲發生。
  5. **文案修正**：畫面上「沒有發現任何共識、矛盾或研究缺口。」（同時漏了差異、也把「沒有結果」講得像是 AI confident 地下了結論）改成「目前沒有產生可用的跨篇比較結果。」，下面再補一行「可能是兩篇研究的重疊範圍較少，或現有單篇分析不足以支持可靠比較。」——因為有了 (3) 的 guard，這個 empty state 現在只可能在「Claude 真的回了合法的空結果」時出現，文案不需要再列出四種 type 名稱，只需要誠實反映「沒有產生」而不是「已確認沒有」。詳見 `src/test/comparisonEmptyResult.test.js`（mock SDK，涵蓋 `normalizeComparisonFindings`／`dropUnsourcedComparisonFindings` 的單元測試、比照使用者實測案例建的兩篇 mock 論文端到端測試、「Claude 真的回空」vs「處理過程中被清空」兩種情境的區分）。
- **跨篇比較 diagnostics 持久化（2026-09-11 第十次）**：上一輪加的 `[PaperLens] Comparison debug` console.debug 有個實際使用上的落差——使用者是比較「跑完之後」才打開 DevTools，所以那次執行的 log 已經沒了，而重新整理只為了再看一次 log 又要重花 API credits。修法是把同一組數字（不是完整 debug 物件，是一份刻意縮減過、只剩 count／typeCounts 的摘要）**跟著 comparison record 一起存進 IndexedDB**，這樣任何一筆已存在的比較結果都能事後回頭查，不需要重新呼叫 API。具體變動：
  - `text.js` 新增 `buildComparisonDiagnostics({rawItemCount, normalizedItemCount, rejections, invalidSourceCount, sourceValidatedItemCount, finalFindings})`——純函式，把 pipeline 各階段的原始資料（`diagnostics.rejections` 陣列、`dropUnsourcedComparisonFindings` 的 dropped 清單等）壓縮成純數字物件：`{rawItemCount, parsedItemCount, normalizedItemCount, rejectedItemCount, invalidTypeCount, invalidSourceCount, sourceValidatedItemCount, finalItemCount, typeCounts:{consensus,difference,contradiction,research_gap}}`。**刻意不保留** `rejections` 陣列裡原本可能夾帶的 `value` 欄位（可能是 Claude 回傳的任意字串）、不保留任何 finding 的 title／summary／claim、不保留 API key、prompt 或 PDF 文字——只留數字和四個固定的中文/英文 enum key。某個階段沒有對應資料時一律回傳 `0`，不用 `null` 也不用猜測值。
  - `storage.js: saveComparison()` 新增 optional 欄位 `diagnostics`（呼叫者沒傳就存 `null`），單純 `db.comparisons.put()`，沒有 schema migration、沒有 index 變動。
  - `ComparePage.jsx: handleCompare()` 把原本只在 `if (import.meta.env.DEV)` 裡組出來的那組數字，改成先呼叫 `buildComparisonDiagnostics()` 產生一份 `diagnosticsSummary`（在 processing-error 的 throw 判斷**之前**就算好，這樣 DEV log 跟錯誤訊息都能用到同一份數字），DEV 模式下印出來，然後**不論是否為 DEV 模式**都把這份 summary 一起傳給 `saveComparison()` 存起來——這是刻意的：診斷資料是為了「之後」能查，不能只綁在「當下有沒有開 DevTools」這個條件上。合法空結果（`rawItemCount===0`）一樣會存 `diagnostics`（`rawItemCount:0, finalItemCount:0`），讓使用者事後能證明「這是 Claude 自己回空，不是本機處理搞丟的」。processing error（`rawItemCount>0` 但驗證後全部無法使用）維持上一輪的行為：直接 `throw`，**完全不呼叫 `saveComparison()`**，所以這種情況不會有 diagnostics 被存下來，也不會被誤存成一筆「正常」的空比較——錯誤訊息本身也附上了原始筆數（例如「（原始 2 筆，驗證後可用 0 筆）」），不需要開 DevTools 也能看到基本數字。
  - `ComparePage.jsx` 新增一個 dev-only、非常低調的「查看診斷資訊」文字按鈕（`import.meta.env.DEV` 包住，production／GitHub Pages 完全不會 render 這段），點開後用純文字列出 Raw／Normalized／Source validated／Final 與四種類型各自的數量；沒有 `diagnostics` 的舊資料點開會顯示「此筆比較沒有診斷資訊（舊資料，或儲存時尚未記錄）。」，不會 crash。
  - **舊資料完全相容**：`diagnostics` 是 optional 欄位，`saveComparison()` 沒收到就存 `null`；`db.comparisons.put()` 寫進去之前就存在、完全沒有這個 key 的舊紀錄也不受影響（讀回來 `record.diagnostics` 是 `undefined`，跟顯式存 `null` 的新紀錄用同一套 optional-chaining 處理，UI 兩種情況都不會 crash）。`exportBackup()`／`importBackup()` 對 `comparisons` 是整包 `toArray()`／逐筆 `put()`，不會挑欄位，所以新增這個 optional 欄位不需要改 backup 邏輯，新舊資料都能正常 round-trip。詳見 `src/test/comparisonDiagnosticsPersistence.test.js`（`buildComparisonDiagnostics` 的純函式測試、合法空結果與 processing-error 兩種情境各自的持久化行為、舊資料沒有 `diagnostics` 時的安全存取、以及 backup/restore round-trip）。
- **跨篇比較的 comparisonStatus／emptyReason 機制（2026-09-11 第十一次）**：靠 diagnostics 拿到確鑿證據後，使用者拿兩篇明顯有重疊（同樣在講 wearable physiological sensing／stress detection，一篇偏 review of wearable stress monitoring、一篇偏 PPG deep learning）的論文比較，結果是 `Raw:0 / Normalized:0 / Source validated:0 / Final:0`——**證實這次不是 normalization、source validation 或 UI 問題，是 Claude 原始 structured output 本身就是 0 findings**。上一輪只用「軟性勸說」（prompt 文字說「少而準不代表空」）處理這個風險，但沒有結構性機制能區分「Claude 真的判斷沒東西可比」跟「模型單純標準過嚴而放棄」，也沒辦法強迫模型解釋為什麼。這一輪的核心改動：
  1. **推理順序：先找可比較主題，再分類**：`COMPARISON_SYSTEM_PROMPT` 新增規則 4，要求 Claude 在判斷 consensus／difference／contradiction／research_gap 之前，先在內部（不輸出這個過程）找出「可比較主題」——同時出現在至少兩篇論文清單裡的主題，或某篇論文的選擇跟另一篇形成有意義對比之處，並列出具體主題清單（research scope、research objective、methodology、physiological signals、wearable devices、datasets／data collection、preprocessing、machine learning models、evaluation、real-world deployment、limitations、future research）。明確禁止「一開始就用有沒有強烈共識/矛盾當篩選標準」，因為那個順序太容易因標準過嚴而整批回空。
  2. **放寬 consensus／difference 的定義，同時保持 contradiction 嚴格**：規則 5 重寫——consensus 不再要求文字、研究設計或結論完全相同，只要兩篇對同一可比較主題有一致的「大方向觀察」且各自有清單重點支持即可；difference 新增大量具體合法範例（涵蓋訊號不同、對裝置的關注程度不同、模型分析深度不同、dataset 納入策略不同、inclusion/exclusion criteria 不同、real-world validation 討論角度不同），並明講「不需要造成相反結論」；contradiction 維持原本嚴格定義完全不變（只有「同一問題／現象／同類條件」下實質不相容才算，方法/範圍不同不構成 contradiction），research_gap 明確要求必須由至少兩篇論文的重點共同支持、綜合出來，不能像某篇論文自己講的。規則 9 同時明講反方向的防呆：不需要四種類型都出現、不需要固定數量、0 個 contradiction／0 個 research_gap 都正常，避免「不要回空」的壓力矯枉過正變成硬湊/幻覺。
  3. **新增 top-level `comparisonStatus`／`emptyReason` 欄位，並且本機驗證兩者是否互相一致**：`prompts.js` 新增 `COMPARISON_STATUSES = ['compared', 'insufficient_overlap']`，`COMPARISON_TOOL` 的 `input_schema` 新增這兩個欄位（`comparisonStatus` 是 required enum；`emptyReason` 在 schema 裡不是 required——JSON Schema 沒辦法乾淨表達「只有在某個值時才 required」這種條件式規則，所以改用程式碼驗證，跟這個專案其他地方處理條件式業務規則的做法一致）。新增 `validateComparisonStatus({comparisonStatus, emptyReason, findingsCount})`（純函式）：`findings` 不為空時 `comparisonStatus` 必須是 `"compared"`；為空時必須是 `"insufficient_overlap"` 且 `emptyReason` 不能是空字串／純空白；任何不一致（包含 enum 值本身不合法）都回傳 `{valid:false, reason}`。`api.js: runComparison()` 在拿到 `callStructured()` 結果後立刻呼叫這個驗證（用 post-normalize 的 `data.findings.length`，不是本機 source 驗證後的數字，因為那是 ComparePage 才有的資訊），不一致就直接 throw「AI 回傳的比較結果狀態不一致……」——**不信任 Claude 自己講的話，這是這個 app 從頭到尾的一貫原則**（跟 `verifyQuote` 不信任 Claude 說的頁碼、`verifyComparisonSources` 不信任 Claude 說的來源 ID 是同一種設計）。這個檢查點剛好比 ComparePage 既有的「raw>0 但 source 驗證後 final=0」guard 更早一步，能額外攔到「Claude 自報 findings 但 type 全部不合法、被 `normalizeComparisonFindings` 清空」這種情況（見 `src/test/comparisonEmptyResult.test.js` 的 scenario C，這次已改寫成直接測 `runComparison` reject，而不是測 ComparePage 才會抓到）。
  4. **emptyReason 有嚴格的「不能偷懶」規則，且不會被當成新的研究結論**：規則 11 要求 Claude 在標成 `insufficient_overlap` 之前，必須先依序確認六項檢查（研究範圍差異／方法差異／資料差異／共同研究主題／共同限制或未來方向／是否至少能形成一個有來源支持的 difference），只要任一項成立就不該回空；同時明講「兩篇不是完全同一主題」本身不構成理由。`emptyReason` 只能描述「為什麼無法比較」，不能塞入新結論、不需要來源 ID——跟其他 `summary` 欄位一樣，是 prompt 約束下的可信文字，但不是本機逐字核對過的引用。
  5. **UI／持久化**：`storage.js: saveComparison()` 新增 optional 欄位 `emptyReason`（只有在 `findings` 為空時才會有內容，跟 `diagnostics` 分開存，因為它是要顯示給使用者看的實際文字內容，不是純數字摘要）；`text.js: buildComparisonDiagnostics()` 新增 `comparisonStatus` 參數，一起存進 `diagnostics`（就是一個短 enum 字串，不算敏感內容）。`ComparePage.jsx` 的空結果畫面從固定文案「可能是兩篇研究的重疊範圍較少……」改成優先顯示 `c.emptyReason`（Claude 自己給的、已通過一致性驗證的理由），只有舊資料（沒有 `emptyReason` 欄位）才 fallback 回原本的通用文案；dev-only 的診斷面板也加了一行顯示 `comparisonStatus`。**舊資料完全相容**：`emptyReason`／`diagnostics.comparisonStatus` 都是 optional，沒有這兩個欄位的舊紀錄讀回來就是 `undefined`／`null`，UI 用同一套 `||` fallback／optional-chaining 處理，不 crash、不需要 migration。
  6. **token budget 沒有再動**：這次 Raw=0 不是 `max_tokens` 截斷（使用者也在需求裡明講不要因為這個問題再調），`COMPARISON_MAX_TOKENS` 維持上一輪的 8192。詳見 `src/test/comparisonStatus.test.js`（`validateComparisonStatus` 純函式測試涵蓋所有一致/不一致組合、`runComparison` 端到端驗證、difference 可獨立存在、`COMPARISON_TOOL` schema 正確要求 `comparisonStatus`、舊資料相容性、diagnostics 儲存 `comparisonStatus` 的 round-trip）；`comparisonTokenBudget.test.js`／`comparisonEmptyResult.test.js` 既有的 mock 回應也同步補上 `comparisonStatus`（否則會被新的一致性檢查擋下來）。
- **跨篇比較的分類語意與來源呈現（2026-09-11 第十二次）**：pipeline 已經能正常產生 comparison cards，但人工驗收發現三個語意問題、一個 UI 問題：
  1. **「共識」relabel 成「共同點」，底層 enum 不變**：`prompts.js: COMPARISON_TYPE_LABELS.consensus` 從「共識」改成「共同點」——跨篇比較裡大多數 consensus 只是「兩篇都提到某主題／都觀察到某方向」，不是兩篇作者明確達成一致結論，「共識」這個詞講得太強、容易讓使用者誤讀。**只改中文顯示，`type` 值仍然是 `consensus`**，完全不用碰任何已存在的比較紀錄——`ComparePage.jsx` 的 `TYPE_LABEL` 一直都是直接重用 `COMPARISON_TYPE_LABELS`（上上輪就做的設計），所以這裡改一個常數，UI 就自動同步，不需要再改 `ComparePage.jsx`。
  2. **新增 `shared_limitation`（共同限制）類型，跟 consensus 明確分開**：使用者發現「兩篇都沒提到 reviewer independent screening」這種「共同缺少某資訊」被歸進 consensus，語意很怪——consensus 應該是「共同都觀察到／都認為」的正面觀察，「共同都缺少／都不足」是相反方向，混在一起會讓使用者誤以為 AI 判斷兩篇作者對某件事有共識，但其實是在講兩篇都沒寫。`prompts.js: COMPARISON_TYPES` 從 4 個擴成 `['consensus','difference','contradiction','shared_limitation','research_gap']` 5 個，`COMPARISON_TYPE_LABELS.shared_limitation = '共同限制'`，`COMPARISON_TOOL` 的 `type` enum／description 同步更新。因為 `normalizeComparisonFindings()` 一直都是用 `COMPARISON_TYPES.includes(finding.type)` 做動態驗證（沒有寫死的 3 選 1 或 4 選 1 清單），新增第 5 個類型完全不需要碰這個函式——這正是當初把 `COMPARISON_TYPES` 設計成「唯一定義、唯一驗證來源」所換來的好處。`text.js: buildComparisonDiagnostics()` 的 `typeCounts` 也改成用 `COMPARISON_TYPES.map(...)` 動態產生（之前是寫死 4 個 key 的物件字面量），同樣的理由——以後再加類型，這裡也不用再改一次。
  3. **`COMPARISON_SYSTEM_PROMPT` 規則 5 新增 shared_limitation 的定義與措辭限制**：明確區分「shared_limitation 是共同都缺少／都不足，跟 consensus 共同都觀察到是相反方向，絕對不要混在一起」；措辭必須保守——由 Claude 綜合多篇缺失資訊推論出來的，一律用「兩篇已保存分析中都未看到...的說明」這類客觀陳述，不能寫成「作者承認」「作者自承」（除非某篇論文的重點裡真的是 kind=fact 且明確是作者自己承認）。也加了产生門檻：不是所有「兩篇都沒提到」的空白都值得產生 shared_limitation，只有跟研究可信度、可復現性、方法透明度、generalizability、evaluation quality、real-world applicability 至少一項有明確關係時才產生，避免「兩篇都沒提作者最喜歡的程式語言」這種無意義的空白也被硬套成一則發現。
  4. **論文類型過度概括的防呆**：使用者實測發現一張卡把 Paper B（scoping review）跟 Paper A（systematic review）一起描述成「兩篇都是 systematic review」。`COMPARISON_SYSTEM_PROMPT` 新增規則 6：如果兩篇論文類型不完全相同，絕對不能把兩篇統稱成其中一種更具體的類型（不能寫「兩篇都是 systematic review」，除非兩篇真的都是），但可以寫「兩篇都是文獻回顧研究」這種不過度精確的說法，或者直接把類型不同本身標成一則 `difference`（範例：「兩篇皆為文獻回顧，但一篇為 systematic review，另一篇為 scoping review。」）；並要求判斷論文類型時優先使用清單重點裡已經明確提到的 paper type／study type／review type 資訊，不要只靠論文標題用猜的。
  5. **來源 chip 加入 analysis item title，並在儲存與渲染兩處都做去重**：舊格式「論文標題 · 面向」在同一面向有兩筆不同來源時完全看不出差別。`ComparePage.jsx: handleCompare()` 新增 `itemTitleByKey`（跟既有的 `moduleByKey`同一個迴圈建立，snapshot 在比較當下的 analysis item title，理由跟 `module` 一樣：之後就算原始 analysis item 被重新分析或刪除，已存的 comparison 來源標籤也不會跟著漂移），存進每個 source 的 `itemTitle` 欄位。`text.js` 新增兩個純函式：`dedupeComparisonSources(sources)`（依 `paperId::itemId` 去重，不是比對顯示文字，保留第一次出現的順序）在儲存前（`sourceVerified` 之後、`dropUnsourcedComparisonFindings` 之前）跟渲染時都各呼叫一次（渲染時再呼叫一次是防禦性的，涵蓋這次修正之前就存在的舊紀錄，冪等不會有副作用）；`formatComparisonSourceLabel({paperTitle, moduleLabel, itemTitle}, {maxSegmentLength=16})` 回傳 `{short, full}`——`short` 是給畫面顯示、超過長度會截斷加「…」的版本，`full` 是完整版本，透過 `<Link title={full}>` 的原生 `title` attribute 做 hover 提示。`itemTitle` 是 optional 參數：沒有值時（舊資料從沒存過這個欄位）`formatComparisonSourceLabel` 會自動退化成舊的兩段式「論文 · 面向」格式，不會顯示空白的第三段，不需要 migration。點擊來源 chip 跳轉的行為完全沒動（還是跳到對應 paper + module 的分析頁籤）——沒有做「scroll 到對應 analysis item」這種進一步的 UI 增強，因為那會牽動 AnalysisModule.jsx 的內部結構，這次的最低要求（來源 paper/module 必須正確）已經有，不需要為了 nice-to-have 擴大改動範圍。
  6. **舊資料完全相容**：舊的比較紀錄裡 `type` 只會是 `consensus`／`difference`／`contradiction`／`research_gap`（沒有 `shared_limitation`），這四個值繼續在 `COMPARISON_TYPE_LABELS`／`ComparePage.jsx` 的 `TYPE_BADGE` 裡，沒有任何 migration，舊卡片照常正確顯示（`consensus` 卡片顯示的中文字從「共識」自動變成「共同點」，這是唯一的視覺差異，資料本身完全沒動）。舊紀錄的 `diagnostics.typeCounts` 沒有 `shared_limitation` 這個 key，DEV 診斷面板用 `?? 0` fallback，不會顯示 `undefined` 或 crash。舊紀錄的 source 沒有 `itemTitle` 欄位，`formatComparisonSourceLabel` 自動退化成兩段式格式。詳見 `src/test/comparisonSharedLimitation.test.js`（共識 relabel、`shared_limitation` 端到端接線、論文類型防呆的 prompt 規則、diagnostics typeCounts、來源 chip 去重與格式化、以及比照使用者實測兩篇論文建的 regression 測試）；`comparisonEmptyResult.test.js` 兩個依賴 `COMPARISON_TYPES.length` 寫死數字（4）的既有測試改成動態讀 `.length`，避免下次再加類型又要手動改數字。
- **跨篇比較的來源追溯問題：ID 合法 ≠ 語意相關（2026-09-12 第十三次）**：前幾輪把 max_tokens、comparisonStatus、分類語意、chip 顯示都修完之後，使用者仔細核對實際卡片內容，發現一個更根本的問題——comparison finding 引用的 source ID 全部都是真實存在的 analysis item，但**來源內容常常根本沒有在講這個 finding 在講的事**。例如一則「共同關注穿戴式生理訊號感測技術的應用潛力」的 finding，來源卻是「搜尋資料庫與檢索日期範圍」跟「PRISMA系統性文獻回顧方法」——兩個都是真實、合法的 item ID，但完全不是在講感測技術的應用潛力。這證明：**既有的 source ID whitelist 驗證只能證明「這個 ID 真的存在」，不能證明「這個來源真的支持這個 finding」**——這兩件事在整個 comparison 程式碼裡從來沒有被清楚區分過，`verifyComparisonSources` 這個名字、`unverifiedSourceCount` 這個欄位，都可能讓人誤以為「通過驗證」代表語意上站得住腳。這一輪的修法明確拒絕了「加一次額外的 Claude API 呼叫做語意驗證」這個選項（使用者明確在意 API 費用，不能為了這個功能默默多花錢），改成三層都是本機、免費、deterministic 的手段：
  1. **prompt 層：先選來源，再寫結論**：`COMPARISON_SYSTEM_PROMPT` 規則 2 全面重寫，明確要求 Claude 在動筆寫 summary 之前，先從至少兩篇不同論文的清單中挑出「內容確實直接相關」的 items，只根據被選中的 items 撰寫 finding；明確禁止「先想好一句 comparison claim，再回頭從合法 ID 裡隨便找幾個看起來相關的來源掛上去湊數」——這是目前實際觀察到的失敗模式的直接對症下藥，但**這只是 prompt 層的行為引導，沒有任何程式碼能強制模型真的照做**，所以後面兩層本機驗證才是真正的防線。
  2. **schema／本機驗證層：sourceClaim 逐字比對**：`COMPARISON_TOOL` 的 `sources` schema 新增必填欄位 `sourceClaim`——要求 Claude 把它選中的那個 item 的 claim 原文逐字複製過來，不能改寫、摘要或翻譯。`text.js: verifyComparisonSources(findings, validItemKeys, claimByKey)` 新增第三個**可選**參數 `claimByKey`：沒傳就是原本的純 ID whitelist 檢查（完全向後相容，舊呼叫端與大部分既有測試完全不用改）；有傳的話，會額外核對 `sourceClaim` 經 `normalizeText()` 正規化空白後，是否跟該 `paperId::itemId` 實際儲存的 claim 完全一致，不一致的來源會被丟棄。**這個函式的 doc comment 現在明確寫清楚「verified」在這裡是什麼意思、不是什麼意思**：只證明「這是一個真實存在、且被正確識別的引用」（trace validity），不能也不會證明「這個來源的內容跟這個 finding 語意相關」（semantic support）——這個區分無法用程式碼自動判斷，只能誠實地不宣稱做到。`ComparePage.jsx: handleCompare()` 新增 `claimByKey`（跟 `moduleByKey`／`itemTitleByKey` 同一個迴圈建立），呼叫 `verifyComparisonSources` 時帶上它。
  3. **跨篇最低門檻：至少 2 篇不同論文**：`text.js` 新增 `dropSinglePaperFindings(findings)`——一則 finding 的（已通過 trace 驗證的）sources 如果全部集中在同一篇論文，代表這根本不是跨篇比較，整則捨棄；`COMPARISON_TOOL` 的 `sources` description 與 `COMPARISON_SYSTEM_PROMPT` 規則 2 也同步要求「有效來源必須涵蓋至少 2 篇不同論文」。`ComparePage.jsx` 在 `dropUnsourcedComparisonFindings` 之後、儲存之前再跑這一步；跟既有的「raw>0 但 final=0」processing-error guard 共用同一套邏輯，如果全部 finding 都因為單篇來源被砍光，一樣會 throw 而不是存成一筆假的空結果。
  4. **shared_limitation 的來源優先序收緊，且不再把「沒有 meta-analysis」自動當弱點**：規則 5 的 shared_limitation 定義加了一段——來源要優先挑真正在講限制／缺失的 items（弱點與延伸、可復現性、方法論裡明確描述方法缺失的 item、或重要的 not_mentioned 缺口），不要拿「與既有回顧的差異比較」這種無關 item 硬湊；並且明講「沒有 p-value／confidence interval／meta-analysis 不代表這本身就是弱點」，只有論文本身聲稱要做 quantitative synthesis、研究目標需要統計合併、或作者自己明講這是 limitation 時才能算 shared_limitation，否則只能中性描述成「兩篇都沒有進行統計合併／meta-analysis」這種事實，不能直接評價成方法缺陷。
  5. **comparison input 補齊 sourceScope／weaknessType**：`prompts.js: buildComparisonUserContent()` 現在會在 item 有 `sourceScope`（數據細節模組）或 `weaknessType`（弱點與延伸模組）時，把它們一起放進送給 Claude 的 XML屬性（沒有就不放，不強塞空字串）；`ComparePage.jsx` 建 `rawItems` 時也同步把這兩個欄位帶上（之前完全沒送）。目的是讓 Claude 判斷「這個 not_mentioned 項目是不是真正的 shared_limitation 候選」時，能直接讀到 weaknessType 這種結構化訊號，不需要自己從 claim 文字重新猜。刻意**不**送完整 evidence quote（維持上一輪對 token 膨脹的防護）。
  6. **UI：來源 chip 旁新增「顯示來源內容」（正式功能，非 DEV-only）**：`ComparePage.jsx` 現在會把每個 source 對應的 `itemClaim`（來自本機 `claimByKey`，不是 Claude 回的那份，即使兩者理論上一致，持久化的永遠是本機權威版本）存進 comparison record。畫面上每則 finding 的來源 chip 旁多一個低調的「顯示來源內容」文字按鈕（一般使用者都看得到，不是 dev-only），點開會列出每個來源 chip 對應的「來源分析內容」原文，讓使用者自己判斷這個來源到底支不支持這個 finding——這是唯一的「語意驗證」防線最終落回人工判讀，而不是假裝程式碼能自動做到。同時把「來源：」改成「來源分析：」，`unverifiedSourceCount` 的警示文字也從「無法在原始資料中驗證」改成「無法比對到正確的來源分析內容」，避免暗示這是完整的語意驗證。措辭上全程避免使用「原文證據」——跨篇比較用的是已保存的單篇 analysis items，不是重新讀 PDF；證據鏈是 comparison finding → 單篇 analysis item →（使用者自己點進該 item）→ 該 item 的 evidence →（再點一次）→ PDF 原文，跨篇比較這一層只到「來源分析」為止。
  7. **diagnostics 新增來源相關計數**：`text.js: buildComparisonDiagnostics()` 新增 `rawSourceCount`（Claude 原始引用的來源總數）、`validSourceIdCount`（ID 存在的來源數）、`validSourceClaimCount`（ID 存在且 sourceClaim 逐字相符的來源數，也就是真正 trace-valid 的數量）、`findingsRejectedForSinglePaperSource`（因為只剩單篇論文來源而被整則捨棄的 finding 數）——全部是數字，不保存任何 claim 文字本身。這四個新欄位的命名刻意呼應「ID valid」vs「claim/trace valid」這組區分，取代了原本可能讓人誤解成「已完整驗證」的籠統用詞。
  8. **max_tokens／comparisonStatus／difference／shared_limitation／既有 diagnostics 欄位／pruneComparisonItems／單篇分析／PDF quote verifier 完全沒動**，`verifyComparisonSources` 的新參數是可選的第三個參數，2-arg 呼叫（絕大多數既有測試）行為完全不變。詳見 `src/test/comparisonSourceTrace.test.js`（Case A-E：合法跨篇來源接受、ID合法但語意無關的來源仍會通過 trace 驗證——測試本身明確標註「trace-valid 不等於 semantic-valid，不能當成好的 fixture」、sourceClaim 不符會被拒絕、單篇來源整則拒絕、雙篇來源正常保留；以及 prompt 規則、tool schema、`buildComparisonUserContent` 的 sourceScope/weaknessType 屬性測試）。**這一版的 sourceClaim 逐字比對機制實際上線後造成了新的問題，下一條記錄（第十四次）已經把它整個拿掉——見下。**
- **撤掉 sourceClaim 逐字比對：過度嚴格導致 9 筆 raw findings 全部被拒（2026-09-12 第十四次）**：上一輪的 sourceClaim 機制上線後，使用者用同樣兩篇論文重新比較，這次 Claude 確實成功產生了 9 筆 findings，但 UI 顯示「比較處理過程發生異常……（原始 9 筆，驗證後可用 0 筆）」——**9 筆全部在 source validation 階段被清空**，不是 prompt 回空、也不是 max_tokens。根本原因幾乎可以肯定是 sourceClaim 逐字比對：要求語言模型把一段 claim 文字一字不差（即使先做過 `normalizeText()` 空白正規化）複製回來極度脆弱——標點符號全形/半形差異、語序微調、模型自己摘要而非逐字複製，都會讓比對失敗；而且這個機制從設計上就無法達成原本的目的：一個真的想引用不相關但合法 ID 的模型，一樣可以把那個不相關 item 的 claim 文字複製得一字不差（複製一段不相關但真實存在的文字，跟複製一段相關的文字，難度完全一樣）——這個機制唯一實際做到的事，就是讓大量真正合法的引用因為措辭差異被錯殺。修法是**整個拿掉這個機制**，回到單純的 ID whitelist 驗證，並新增一個「本機 source resolver」取代「要求 Claude 自己回報內容」：
  1. **撤掉 sourceClaim 要求**：`COMPARISON_TOOL` 的 `sources` schema 拿掉 `sourceClaim` 欄位（`required` 改回 `['paperId', 'itemId']`）；`COMPARISON_SYSTEM_PROMPT` 規則 2 拿掉「把 claim 原文逐字複製過來」的指示，改成明講「sources 只需要提供 paperId 跟 itemId 這兩個欄位，不需要複製 claim 內容——PaperLens 會直接從已保存的資料查出這個來源實際的標題與內容」。**規則 2 的「先選來源、再寫結論」與「有效來源必須涵蓋至少 2 篇不同論文」這兩條刻意保留**——這是目前唯一還在嘗試改善 relevance 的手段（純 prompt 層引導，沒有任何程式碼能強制），使用者也明確要求不要撤掉。
  2. **`text.js: verifyComparisonSources(findings, validItemKeys)` 拿掉第三個 `claimByKey` 參數**，回到純粹的 ID whitelist 檢查（跟兩輪前完全一樣的行為）。函式的 doc comment 重寫，明確記錄「為什麼曾經加過 claim 比對、又為什麼把它拿掉」，避免以後又有人基於同樣的直覺重新加回一樣脆弱的機制。
  3. **新增本機 authoritative source resolver**：`text.js` 新增 `buildComparisonSourceIndex(items)`（把 `paperId::itemId` 對應到完整 comparison-input item 的 Map）與 `resolveComparisonSource(itemIndex, {paperId, itemId})`（回傳 `{paperId, itemId, paperTitle, module, itemTitle, claim, kind, sourceScope, weaknessType, verificationStatus}`）。這取代了原本 `ComparePage.jsx` 裡三個各自維護的 `moduleByKey`／`itemTitleByKey`／`claimByKey` Map——現在只有一個 `sourceIndex`，任何來源顯示用的欄位都從這裡查，**完全不使用 Claude 回應裡的任何內容**（Claude 從頭到尾只被信任提供 `{paperId, itemId}` 這兩個欄位）。`ComparePage.jsx` 儲存 finding 時，每個 source 的 `module`／`itemTitle`／`itemClaim` 都是呼叫 `resolveComparisonSource()` 現查現填，不是相信 Claude 給的任何文字。
  4. **diagnostics 全面改名，補齊使用者要求的欄位，且拿掉「analysis item」與「comparison finding」共用「item」這個詞造成的混淆**：`buildComparisonDiagnostics()` 的欄位全面從 `*ItemCount` 改名成 `*FindingCount`（`rawItemCount`→`rawFindingCount`、`normalizedItemCount`→`normalizedFindingCount`、`rejectedItemCount`→`rejectedFindingCount`、`finalItemCount`→`finalFindingCount`——這裡的「item」一直指的是「一則 comparison finding」，跟「analysis item」完全是两回事，過去共用「item」這個字本身就是一種命名混淆）；新增 `rawSourceCount`、`invalidSourceIdCount`、`findingsAfterIdValidation`、`singlePaperFindingCount`、`findingsAfterMultiPaperValidation`。原本上一輪加的 `validSourceIdCount`／`validSourceClaimCount`／`findingsRejectedForSinglePaperSource` 拿掉，改用使用者這次明確要求的欄位名稱。`sourceClaimMismatchCount` 與 `findingsAfterClaimValidation` 兩個欄位仍然保留在回傳物件裡，但**恆為 0／恆等於 findingsAfterIdValidation**——刻意不整個移除，是為了讓「這個檢查機制已經被拿掉」這件事在 diagnostics 裡看得到，而不是無聲消失。`ComparePage.jsx` 的 DEV 診斷面板同步更新顯示這些新欄位；面板對所有計數欄位用 `?? '—'`（不是 `?? 0`）——因為新舊三種 diagnostics 格式（這一輪／上一輪／上上輪）欄位名稱都不一樣，`0` 代表「這個階段真的算出 0」，`—` 才代表「這筆舊紀錄的格式沒有這個欄位」，兩者不能混為一談。
  5. **文案簡化**：processing-error 的錯誤訊息從一大串列出「不存在來源／內容不符／同篇論文」等內部細節，改成單純一句「比較失敗：產生的結果無法可靠連結到來源分析，請重新比較。」——一般使用者不需要知道內部是哪個 pipeline 階段出的問題，詳細 counts 留給 DEV 診斷面板。單一 finding 的來源警示文字也簡化成「有 X 筆引用來源找不到對應的分析項目，已略過。」，不再提「內容不符」（因為已經不检查內容是否相符了）。
  6. **仍然沒有新增任何額外 API 呼叫**：整個修法完全在 prompt 約束、deterministic ID whitelist、本機 resolver、透明 UI 四者之內完成，使用者在意的 API credits 完全沒有受影響——這跟上一輪的原則一致，只是上一輪多加的「sourceClaim 比對」這個本機機制本身被證明弊大於利，這次拿掉的是這個，不是拿掉 ID 驗證或降低整體嚴謹度。
  7. **舊資料相容**：目前實際存在過的兩種 source 格式——`{paperId, itemId}`（兩輪前）與 `{paperId, itemId, sourceClaim}`（上一輪）——都能正常讀取：`verifyComparisonSources()` 只看 `paperId`／`itemId`，`sourceClaim` 欄位如果還在舊資料裡就單純被忽略，不參與任何驗證、也不會被 UI 顯示（UI 的「查看來源分析」——第十五次改的名字，見下——永遠讀 `resolveComparisonSource()`／本機存的 `itemClaim`，不是 source 物件上可能還留著的舊 `sourceClaim`）。假設性的 `sourceIds: [...]` 扁平陣列格式（使用者提出的替代設計，這個 app 從未真正採用過）也做了防禦性測試，確認不會 crash，只是沒有 sources 可用。詳見 `src/test/comparisonSourceTrace.test.js`（改寫成對應新架構：valid sourceId 不需要 sourceClaim 即可保留、sourceClaim 不符不再導致 reject、UI 一律使用本機 claim、invalid sourceId 仍被拒絕、單篇來源仍被拒絕、雙篇來源正常保留、`resolveComparisonSource` 回傳完整本機資料、舊兩種 source 格式相容、以及最關鍵的 regression：9 筆 raw findings、每筆都有兩篇合法 paperId 的 sourceId，不能再因為 claim 比對而全部變成 0）；`comparisonDiagnosticsPersistence.test.js` 同步更新成新的欄位名稱。
- **跨篇比較最後一輪：可信度誠實呈現（2026-09-12 第十五次）**：ID whitelist 驗證能保證「來源是真的、可追溯」，但沒辦法保證「來源在語意上真的支持這則 finding」——這是 deterministic 驗證的天花板，這次不打算用第二次 Claude 呼叫或本機 embedding model 去突破它（使用者明確不要），而是讓 UI 誠實反映這個界線，讓使用者能快速人工確認。純 UI／文案調整，完全沒有動到 comparison 的核心邏輯：
  1. **頁面頂部新增一行說明**：「跨篇比較是 AI 根據已保存的單篇分析進行綜合整理；來源分析可追溯，但比較結論與來源的語意關係仍建議人工確認。」跟既有的「跨篇比較會根據各篇已保存的分析結果整理...」那句一樣是純 `<p className="muted">`，不是 boxed warning、沒有背景色、沒有紅色。
  2. **每張 comparison finding 卡片新增「來源待確認」badge**：跟既有的 type badge（共同點／差異／矛盾／共同限制／研究缺口）並排顯示，用 `<Badge kind="source_pending">來源待確認</Badge>`——`source_pending` 刻意不是 `ui.jsx: BADGE_VARIANTS` 裡定義過的 key，`Badge` 元件對未知 kind 會自動 fallback 成 `badge-neutral`（淡灰、無底色），不需要為此改 `ui.jsx`。這個 badge 對**每一張**卡片都無條件顯示（不是某個 data 欄位驅動的條件渲染），所以舊 comparison 完全自動套用，不需要 migration、不需要幫舊紀錄補欄位——這正是使用者要求的「UI 層級提醒，不是每筆資料的 persisted status」。
  3. **「顯示來源內容」改名「查看來源分析」／「隱藏來源分析」**，展開後的內容從一行式「來源分析內容（論文）：「claim」」改成四行標籤化格式：「論文：」「面向：」「分析項目：」「來源分析內容：」，並在展開區塊最上方加一行 `faint` 小字「請確認下列來源分析是否真的支持上方的跨篇比較結論。」——同樣不是 alert box，只是一行小字。內容來源完全沒變，仍然是 `resolveComparisonSource()` 解析出的本機權威 `itemClaim`，不是 Claude 回應裡的任何欄位（上一輪就已經是這樣，這次只是重新排版顯示）。
  4. **全面稽核 comparison UI 的用詞**：確認 `ComparePage.jsx` 裡沒有出現「已驗證」「原文證據」「已證實」「來源已核對」這類過強字眼——稽核後確認本來就沒有（`verified`／`sourceVerified` 只是內部變數名稱，從未顯示給使用者；`f.unverifiedSourceCount` 的警示文字也已經在上一輪改成「找不到對應的分析項目」）。維持並強化既有的用詞區分：跨篇比較這一層只能講「來源分析」「來源可追溯」「來源待確認」，不能講「已驗證」——因為 comparison source provenance（ID 是否存在、對應到哪個 analysis item）跟單篇分析的 quote verification（`verifyQuote()` 真的在本機 PDF parsed text 上逐字核對）是兩個完全不同層級的保證，後者才能講「原文已核對」。**這次完全沒有碰 `AnalysisModule.jsx`／`ui.jsx` 的 `BADGE_VARIANTS`**——「原文已核對」「部分符合」「原文找不到這句」三個單篇分析的 label 原封不動。
  5. **沒有新增任何 API 呼叫、沒有新 schema、沒有 migration**：「查看來源分析」按鈕的 `toggleSourceContent()` 純粹是本機 state setter（`setExpandedSourceContentKeys`），不含 `await`、不呼叫 `runComparison`／`logUsage`／任何網路請求；「來源待確認」badge 不寫進 `comparison.diagnostics` 或任何新欄位，純粹是渲染時的固定文字。`COMPARISON_MAX_TOKENS`、comparison prompt、`comparisonStatus`／`emptyReason`、五種分類、source ID whitelist、`dropSinglePaperFindings`、`pruneComparisonItems`、source resolver、diagnostics pipeline、storage schema、單篇分析、quote verifier 全部沒有變動。詳見 `src/test/compareUiCopy.test.js`——這個專案一貫不做 component test（見 `comparison.test.js` 開頭註解），但這次的需求本質是「精確的 UI 文案與禁用詞」，沒有對應的純函式可以測，所以改用讀取 `ComparePage.jsx`／`ui.jsx` 原始碼文字做 regression guard（明確標註這不是真正的 component test，只是鎖定文案與禁用詞、以及用簡單的字串手法驗證「查看來源分析」按鈕的 handler 不含 `await`／API 呼叫關鍵字），涵蓋：badge 一律顯示、badge 不寫入資料庫、單篇分析三個 label 完全沒被改動或影子覆蓋、按鈕改名且 handler 無 API 呼叫、展開內容的四個標籤欄位與資料來源（`s.itemClaim` 不是 `s.sourceClaim`）、確認提示文字存在且非 boxed alert、來源 chip 連結未變、七個禁用詞都不存在、頁首說明文字存在且為 muted 而非 boxed。
- **部署前安全稽核（2026-09-12）**：全專案搜尋 `sk-ant-` 與通用 secret pattern，確認沒有真實 API key（只有明顯假的測試 fixture 字串與 `SettingsPage.jsx` 的 input placeholder）；production bundle 內容也直接檢查過，DEV-only 診斷字串（「查看診斷資訊」等）確認完全沒有被打包進去。`.gitignore` 原本沒有排除 `.env` 系列（專案目前沒有任何 `.env` 檔案，屬於預防性修正）——已新增 `.env`／`.env.*`。當時這個目錄還沒有 `.git`，所以「已追蹤檔案」「git history」兩項不適用。`npm audit` 的 8 個弱點全部集中在 `jsdom`／`vite`／`vitest` 這三個 devDependency 的傳遞依賴鏈（`esbuild`／`canvas`／`@mapbox/node-pre-gyp`／`tar`／`@vitest/mocker`），修復都需要 major 版本升級，且都不影響 production bundle（已用 bundle 內容比對確認 unused）——依照指示沒有執行 `npm audit fix --force`，也沒有做任何 major 升級。Vite `base: './'`、`deploy.yml`（`npm ci`→`npm test`→`npm run build`→`upload-pages-artifact`→`deploy-pages`）、備份/還原的 API key 排除邏輯、`clearAllPaperData()` 不碰 `settings` 表，全部稽核後確認正確，沒有修改。這次唯一的程式碼變更就是上面那行 `.gitignore`。
- **GitHub Pages production 上 PDF.js worker 載入失敗（2026-09-12 第十六次）**：部署到 `https://chenhongchen1006.github.io/paper-lens/` 後，實際上傳 PDF 出現「PDF 解析失敗：Setting up fake worker failed: "Failed to fetch dynamically imported module: https://.../assets/pdf.worker.min-GB3t0DcA.mjs"」，本機 `http://127.0.0.1:5173/` 正常。**深入稽核後，沒有在程式碼裡找到實際的 bug**：
  1. **worker URL 建構方式原本就已經是正確的**：`src/lib/pdf.js` 本來就是用 `import pdfjsWorkerUrl from 'pdfjs-dist/build/pdf.worker.min.mjs?url'`（Vite 的 `?url` asset import），不是手動組合路徑。實際檢查 build 產物（`dist/assets/index-*.js`）確認 Vite 把它編譯成 `new URL("pdf.worker.min-<hash>.mjs", import.meta.url).href`——**用這個 JS chunk 檔案自己的 URL（`import.meta.url`）當基準去解析**，不是 `window.location`，不需要任何 username/repo 名稱，跟 GitHub Pages 子路徑完全無關，會自動正確。`cMapUrl`／`standardFontDataUrl` 也已經是用 `new URL(relativePath, document.baseURI)`（`document.baseURI` 本身就包含 `/paper-lens/` 這個子路徑前綴），同樣不需要修改。
  2. **直接對正式站發 HTTP 請求驗證，確認目前並沒有這個 bug**：`curl -I` 實際打 `https://chenhongchen1006.github.io/paper-lens/assets/pdf.worker.min-GB3t0DcA.mjs`，回應是 `200`、`content-type: text/javascript; charset=utf-8`、`access-control-allow-origin: *`——MIME type 正確、CORS 開放、檔案確實存在；`pdf-cmaps/`、`pdf-standard-fonts/` 底下的檔案也都用同樣方式驗證過，正常回應。也確認正式站目前的 `index.html` 與其引用的 JS chunk 裡的 worker hash（`GB3t0DcA`）跟 `dist/` 裡實際的檔案完全一致，沒有版本不同步的問題。**因為 pdfjs-dist 內部的「real Worker 失敗 → 退回同執行緒動態 import 同一個 URL」這個 fallback 機制剛好會讓兩種載入方式失敗時報出幾乎一樣的錯誤訊息**（見 `node_modules/pdfjs-dist/build/pdf.mjs` 的 `PDFWorker._initialize()`／`_setupFakeWorker()`），符合使用者回報的症狀；但這個 fallback 本身就是對同一個 URL 再打一次，對「暫時性」的失敗完全沒有防護力。综合以上，**最可能的解釋是 GitHub Pages CDN 在部署完成後短暫的邊緣節點同步延遲**（GitHub 官方文件本身就提到部署後可能要幾分鐘才會全球同步），或使用者瀏覽器的隱私／廣告阻擋擴充功能封鎖了這個請求——這兩種情況都無法從程式碼層面「修好」，也不應該假裝可以修好。
  3. **修法：不改 URL 建構方式（本來就對），改善使用者遇到這個失敗類別時看到的訊息**：`pdf.js` 新增並 export `translatePdfError(err)`——只辨識 pdf.js 這類 worker 載入失敗（比對 `fake worker`／`dynamically imported module` 這兩個 pdf.js 自己固定會用的字眼，不是隨便的字串比對），回傳「PDF 解析元件載入失敗，可能是暫時的網路問題，或瀏覽器的隱私／廣告阻擋擴充功能封鎖了這個請求。請重新整理頁面後再試一次；如果持續發生，可以嘗試暫時停用相關擴充功能，或換一個瀏覽器測試。」；其他類型的錯誤（例如真的毀損的 PDF）原樣通過，不受影響。`ingest.js`（`extractPdfText` 失敗的地方，也就是使用者回報那句「PDF 解析失敗：...」的來源）與 `OcrPanel.jsx`（`renderPageToImage` 也走同一個 worker）都接上這個翻譯——`OcrPanel.jsx` 原本一個 catch block 同時處理 pdf.js 錯誤跟 Claude API 錯誤，`translatePdfError()` 對不符合 worker 失敗特徵的訊息（包括 Claude API 錯誤）完全不改動，所以套用在共用 catch block 上是安全的。
  4. **沒有動到 vite.config.js**：`base: './'` 保留，稽核確認這正是讓 worker／cMap／字型路徑都能在任何 GitHub username 或 repo 名稱下正確運作的原因，不需要為了這次的問題改成寫死路徑。
  5. **測試**：`src/test/pdfWorkerAssets.test.js`——純函式測試 `translatePdfError()` 正確辨識/不辨識各種訊息；原始碼層級的 regression guard（`pdf.js` 用 `?url` import、沒有寫死 `localhost`/`127.0.0.1`、沒有寫死 root-absolute 的 `/assets/...` 或 `/pdf-worker`、`assetUrl()` 用 `document.baseURI` 不是 `window.location.origin`、`vite.config.js` 仍是 `base: './'`）；以及**條件式**的 build 產物檢查（`dist/` 存在時才跑：worker 檔案確實存在、main bundle 用 `import.meta.url` 相對解析而不是 root-absolute 字串、`index.html` 沒有寫死 localhost、`pdf-cmaps`／`pdf-standard-fonts` 確實被複製進 dist）——因為 `deploy.yml` 是先 `npm test` 才 `npm run build`，CI 跑測試當下 `dist/` 通常還不存在，這部分用 `describe.runIf`/`describe.skipIf` 優雅跳過而不是失敗，明確標註「這不是真正驗證了 GitHub Pages 的實際 HTTP hosting，那部分是用 `curl` 對正式站直接驗證的，不是靠 unit test」。
